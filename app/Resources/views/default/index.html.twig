{% extends 'base.html.twig' %}

{% block body %}

{{ form(form) }}

{% if(name is defined) %}
    {{ include("default/_template.html.twig") }}

    <textarea style="width: 500px; height: 200px;">{{ include("default/_template.html.twig") }}</textarea>
{% endif %}

{% endblock %}
